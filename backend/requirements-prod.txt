# Runtime-only dependencies. The model artifact is bundled with the image;
# pandas, scikit-learn, SHAP, and pytest are only needed for local retraining/tests.
fastapi>=0.100.0
uvicorn>=0.22.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
numpy>=1.24.0
xgboost>=1.7.0
